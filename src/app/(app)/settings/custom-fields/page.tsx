import SettingsPlaceholder from "@/components/SettingsPlaceholder";

export default function CustomFieldsSettingsPage() {
  return (
    <SettingsPlaceholder
      title="Custom Fields"
      description="Define custom event and client fields, and custom date definitions used in merge tags and helpers."
    />
  );
}

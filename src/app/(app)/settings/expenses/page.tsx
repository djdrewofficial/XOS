import SettingsPlaceholder from "@/components/SettingsPlaceholder";

export default function ExpensesSettingsPage() {
  return (
    <SettingsPlaceholder
      title="Expenses"
      description="Manage expense categories and default payees used across event expenses."
    />
  );
}

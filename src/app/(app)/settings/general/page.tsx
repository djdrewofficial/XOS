import SettingsPlaceholder from "@/components/SettingsPlaceholder";

export default function GeneralSettingsPage() {
  return (
    <SettingsPlaceholder
      title="General"
      description="Company-wide settings: business name, contact info, timezone, branding, and app defaults."
    />
  );
}

import SettingsPlaceholder from "@/components/SettingsPlaceholder";

export default function StaffSettingsPage() {
  return (
    <SettingsPlaceholder
      title="Staff Settings"
      description="Roles, permission tiers, pay defaults, and staff-portal options."
    />
  );
}

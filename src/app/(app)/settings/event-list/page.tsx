import SettingsPlaceholder from "@/components/SettingsPlaceholder";

export default function EventListSettingsPage() {
  return (
    <SettingsPlaceholder
      title="Event List Settings"
      description="Default columns, sorting, and saved views for the events list."
    />
  );
}
